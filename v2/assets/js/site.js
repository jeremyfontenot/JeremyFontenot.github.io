
const navToggle=document.querySelector('.nav-toggle');const navLinks=document.querySelector('.nav-links');if(navToggle&&navLinks){navToggle.addEventListener('click',()=>{const open=navLinks.classList.toggle('open');navToggle.setAttribute('aria-expanded',String(open));});navLinks.addEventListener('click',e=>{if(e.target instanceof HTMLAnchorElement){navLinks.classList.remove('open');navToggle.setAttribute('aria-expanded','false')}});document.addEventListener('keydown',e=>{if(e.key==='Escape'){navLinks.classList.remove('open');navToggle.setAttribute('aria-expanded','false')}})}
const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;const reveal=[...document.querySelectorAll('[data-reveal]')];if(reveal.length){if(reduced||!('IntersectionObserver'in window)){reveal.forEach(n=>n.classList.add('visible'))}else{const io=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');io.unobserve(entry.target)}})},{threshold:.12,rootMargin:'0px 0px -8% 0px'});reveal.forEach(n=>io.observe(n));}}
const counters=[...document.querySelectorAll('[data-count]')];if(counters.length){const set=(el)=>{const to=Number(el.dataset.count||0);if(reduced){el.textContent=String(to);return}let start=null;const step=t=>{if(!start)start=t;const p=Math.min(1,(t-start)/900);el.textContent=String(Math.round(to*p));if(p<1)requestAnimationFrame(step)};requestAnimationFrame(step)};const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){set(e.target);io.unobserve(e.target)}}),{threshold:.3});counters.forEach(c=>io.observe(c));}
