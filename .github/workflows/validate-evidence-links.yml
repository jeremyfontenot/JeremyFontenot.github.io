name: Validate Evidence Links

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  validate-links:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate evidence/public links
        shell: pwsh
        run: |
          ./scripts/validate-evidence-links.ps1
